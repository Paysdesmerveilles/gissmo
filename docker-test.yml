db:
  image: postgres
  environment:
    - POSTGRES_USER=gissmo
    - POSTGRES_PASSWORD=gissmo
  ports:
    - "5433:5432"
web:
  build: .
  command: test
  ports:
    - "8000:8001"
  links:
    - db
  environment:
    - SECRET_KEY="abcdefghijklmnopqrstuvwxyz"
